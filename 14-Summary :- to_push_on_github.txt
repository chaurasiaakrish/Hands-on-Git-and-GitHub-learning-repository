git init -> git add . -> git commit -> git remote add origin <URL> -> git push -u origin main


The full process of committing and pushing for the first time :
Initialize repo → git init
Track files → git add .
Save changes → git commit
Connect to GitHub → git remote add origin <URL>
Push for the first time → git push -u origin main